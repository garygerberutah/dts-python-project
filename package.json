{
  "name": "ggp3d",
  "version": "0.1.0",
  "description": "Zero-dependency 3D geometry processor — native Web Components + Rust WASM",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test frontend/components/*.test.js",
    "lint": "eslint frontend/ --ext .js --max-warnings 0",
    "format": "prettier --write 'frontend/**/*.js' 'frontend/**/*.css' 'templates/**/*.j2'"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "jsdom": "^25.0.0",
    "prettier": "^3.2.0"
  }
}
